apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-181
spec:
  selector:
    matchLabels:
      app: app-181
  template:
    metadata:
      labels:
        app: app-181
    spec:
      containers:
      - image: nginx
        name: nginx
