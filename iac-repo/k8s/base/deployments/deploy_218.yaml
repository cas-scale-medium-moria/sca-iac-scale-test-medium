apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-217
spec:
  selector:
    matchLabels:
      app: app-217
  template:
    metadata:
      labels:
        app: app-217
    spec:
      containers:
      - image: nginx
        name: nginx
