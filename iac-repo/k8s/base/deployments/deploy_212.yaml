apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-211
spec:
  selector:
    matchLabels:
      app: app-211
  template:
    metadata:
      labels:
        app: app-211
    spec:
      containers:
      - image: nginx
        name: nginx
