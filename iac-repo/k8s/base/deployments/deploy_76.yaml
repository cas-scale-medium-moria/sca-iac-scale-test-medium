apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-75
spec:
  selector:
    matchLabels:
      app: app-75
  template:
    metadata:
      labels:
        app: app-75
    spec:
      containers:
      - image: nginx
        name: nginx
