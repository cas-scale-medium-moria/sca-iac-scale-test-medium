apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-431
spec:
  selector:
    matchLabels:
      app: app-431
  template:
    metadata:
      labels:
        app: app-431
    spec:
      containers:
      - image: nginx
        name: nginx
