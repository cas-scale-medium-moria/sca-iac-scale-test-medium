apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-204
spec:
  selector:
    matchLabels:
      app: app-204
  template:
    metadata:
      labels:
        app: app-204
    spec:
      containers:
      - image: nginx
        name: nginx
