apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-440
spec:
  selector:
    matchLabels:
      app: app-440
  template:
    metadata:
      labels:
        app: app-440
    spec:
      containers:
      - image: nginx
        name: nginx
