apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-482
spec:
  selector:
    matchLabels:
      app: app-482
  template:
    metadata:
      labels:
        app: app-482
    spec:
      containers:
      - image: nginx
        name: nginx
