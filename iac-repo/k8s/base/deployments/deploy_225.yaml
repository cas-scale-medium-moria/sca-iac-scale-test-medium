apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-224
spec:
  selector:
    matchLabels:
      app: app-224
  template:
    metadata:
      labels:
        app: app-224
    spec:
      containers:
      - image: nginx
        name: nginx
