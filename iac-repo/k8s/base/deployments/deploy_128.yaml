apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-127
spec:
  selector:
    matchLabels:
      app: app-127
  template:
    metadata:
      labels:
        app: app-127
    spec:
      containers:
      - image: nginx
        name: nginx
