apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-499
spec:
  selector:
    matchLabels:
      app: app-499
  template:
    metadata:
      labels:
        app: app-499
    spec:
      containers:
      - image: nginx
        name: nginx
