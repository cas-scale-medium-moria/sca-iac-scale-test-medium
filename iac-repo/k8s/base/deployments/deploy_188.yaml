apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-187
spec:
  selector:
    matchLabels:
      app: app-187
  template:
    metadata:
      labels:
        app: app-187
    spec:
      containers:
      - image: nginx
        name: nginx
