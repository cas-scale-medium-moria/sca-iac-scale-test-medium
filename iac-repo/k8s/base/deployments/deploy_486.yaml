apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-485
spec:
  selector:
    matchLabels:
      app: app-485
  template:
    metadata:
      labels:
        app: app-485
    spec:
      containers:
      - image: nginx
        name: nginx
