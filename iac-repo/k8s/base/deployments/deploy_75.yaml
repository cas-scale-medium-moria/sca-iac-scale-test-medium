apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-74
spec:
  selector:
    matchLabels:
      app: app-74
  template:
    metadata:
      labels:
        app: app-74
    spec:
      containers:
      - image: nginx
        name: nginx
