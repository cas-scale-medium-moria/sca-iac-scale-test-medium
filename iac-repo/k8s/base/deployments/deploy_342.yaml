apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-341
spec:
  selector:
    matchLabels:
      app: app-341
  template:
    metadata:
      labels:
        app: app-341
    spec:
      containers:
      - image: nginx
        name: nginx
