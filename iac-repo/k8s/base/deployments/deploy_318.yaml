apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-317
spec:
  selector:
    matchLabels:
      app: app-317
  template:
    metadata:
      labels:
        app: app-317
    spec:
      containers:
      - image: nginx
        name: nginx
