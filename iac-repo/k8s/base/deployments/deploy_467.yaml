apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-466
spec:
  selector:
    matchLabels:
      app: app-466
  template:
    metadata:
      labels:
        app: app-466
    spec:
      containers:
      - image: nginx
        name: nginx
