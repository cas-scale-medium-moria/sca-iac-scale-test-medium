apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-408
spec:
  selector:
    matchLabels:
      app: app-408
  template:
    metadata:
      labels:
        app: app-408
    spec:
      containers:
      - image: nginx
        name: nginx
