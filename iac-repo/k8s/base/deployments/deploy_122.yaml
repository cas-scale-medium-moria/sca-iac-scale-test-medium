apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-121
spec:
  selector:
    matchLabels:
      app: app-121
  template:
    metadata:
      labels:
        app: app-121
    spec:
      containers:
      - image: nginx
        name: nginx
