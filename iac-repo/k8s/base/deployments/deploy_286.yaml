apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-285
spec:
  selector:
    matchLabels:
      app: app-285
  template:
    metadata:
      labels:
        app: app-285
    spec:
      containers:
      - image: nginx
        name: nginx
