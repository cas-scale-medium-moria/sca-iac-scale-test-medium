apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-142
spec:
  selector:
    matchLabels:
      app: app-142
  template:
    metadata:
      labels:
        app: app-142
    spec:
      containers:
      - image: nginx
        name: nginx
