apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-365
spec:
  selector:
    matchLabels:
      app: app-365
  template:
    metadata:
      labels:
        app: app-365
    spec:
      containers:
      - image: nginx
        name: nginx
