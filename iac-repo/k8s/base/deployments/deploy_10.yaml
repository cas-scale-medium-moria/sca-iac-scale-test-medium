apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-9
spec:
  selector:
    matchLabels:
      app: app-9
  template:
    metadata:
      labels:
        app: app-9
    spec:
      containers:
      - image: nginx
        name: nginx
