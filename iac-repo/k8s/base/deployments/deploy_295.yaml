apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-294
spec:
  selector:
    matchLabels:
      app: app-294
  template:
    metadata:
      labels:
        app: app-294
    spec:
      containers:
      - image: nginx
        name: nginx
