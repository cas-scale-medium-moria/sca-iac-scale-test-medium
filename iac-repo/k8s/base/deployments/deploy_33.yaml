apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-32
spec:
  selector:
    matchLabels:
      app: app-32
  template:
    metadata:
      labels:
        app: app-32
    spec:
      containers:
      - image: nginx
        name: nginx
