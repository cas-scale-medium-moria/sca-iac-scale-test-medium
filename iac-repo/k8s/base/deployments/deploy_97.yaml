apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-96
spec:
  selector:
    matchLabels:
      app: app-96
  template:
    metadata:
      labels:
        app: app-96
    spec:
      containers:
      - image: nginx
        name: nginx
