apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-186
spec:
  selector:
    matchLabels:
      app: app-186
  template:
    metadata:
      labels:
        app: app-186
    spec:
      containers:
      - image: nginx
        name: nginx
