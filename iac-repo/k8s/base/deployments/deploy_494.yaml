apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-493
spec:
  selector:
    matchLabels:
      app: app-493
  template:
    metadata:
      labels:
        app: app-493
    spec:
      containers:
      - image: nginx
        name: nginx
