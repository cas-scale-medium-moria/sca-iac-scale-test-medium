apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-300
spec:
  selector:
    matchLabels:
      app: app-300
  template:
    metadata:
      labels:
        app: app-300
    spec:
      containers:
      - image: nginx
        name: nginx
