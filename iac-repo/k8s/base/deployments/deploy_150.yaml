apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-149
spec:
  selector:
    matchLabels:
      app: app-149
  template:
    metadata:
      labels:
        app: app-149
    spec:
      containers:
      - image: nginx
        name: nginx
