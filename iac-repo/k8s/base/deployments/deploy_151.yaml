apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-150
spec:
  selector:
    matchLabels:
      app: app-150
  template:
    metadata:
      labels:
        app: app-150
    spec:
      containers:
      - image: nginx
        name: nginx
