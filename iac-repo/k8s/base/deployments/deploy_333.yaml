apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-332
spec:
  selector:
    matchLabels:
      app: app-332
  template:
    metadata:
      labels:
        app: app-332
    spec:
      containers:
      - image: nginx
        name: nginx
