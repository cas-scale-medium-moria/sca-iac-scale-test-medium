apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-206
spec:
  selector:
    matchLabels:
      app: app-206
  template:
    metadata:
      labels:
        app: app-206
    spec:
      containers:
      - image: nginx
        name: nginx
