apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-315
spec:
  selector:
    matchLabels:
      app: app-315
  template:
    metadata:
      labels:
        app: app-315
    spec:
      containers:
      - image: nginx
        name: nginx
