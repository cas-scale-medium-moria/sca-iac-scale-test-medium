apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-393
spec:
  selector:
    matchLabels:
      app: app-393
  template:
    metadata:
      labels:
        app: app-393
    spec:
      containers:
      - image: nginx
        name: nginx
