apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-50
spec:
  selector:
    matchLabels:
      app: app-50
  template:
    metadata:
      labels:
        app: app-50
    spec:
      containers:
      - image: nginx
        name: nginx
