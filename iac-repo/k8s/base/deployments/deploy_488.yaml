apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-487
spec:
  selector:
    matchLabels:
      app: app-487
  template:
    metadata:
      labels:
        app: app-487
    spec:
      containers:
      - image: nginx
        name: nginx
