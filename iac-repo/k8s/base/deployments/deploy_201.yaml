apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-200
spec:
  selector:
    matchLabels:
      app: app-200
  template:
    metadata:
      labels:
        app: app-200
    spec:
      containers:
      - image: nginx
        name: nginx
