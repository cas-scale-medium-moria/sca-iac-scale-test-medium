apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-227
spec:
  selector:
    matchLabels:
      app: app-227
  template:
    metadata:
      labels:
        app: app-227
    spec:
      containers:
      - image: nginx
        name: nginx
