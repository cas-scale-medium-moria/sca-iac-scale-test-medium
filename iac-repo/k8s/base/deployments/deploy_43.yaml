apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-42
spec:
  selector:
    matchLabels:
      app: app-42
  template:
    metadata:
      labels:
        app: app-42
    spec:
      containers:
      - image: nginx
        name: nginx
