apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-356
spec:
  selector:
    matchLabels:
      app: app-356
  template:
    metadata:
      labels:
        app: app-356
    spec:
      containers:
      - image: nginx
        name: nginx
