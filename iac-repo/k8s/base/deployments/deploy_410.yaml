apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-409
spec:
  selector:
    matchLabels:
      app: app-409
  template:
    metadata:
      labels:
        app: app-409
    spec:
      containers:
      - image: nginx
        name: nginx
