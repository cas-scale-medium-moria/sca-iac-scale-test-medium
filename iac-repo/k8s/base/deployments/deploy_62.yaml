apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-61
spec:
  selector:
    matchLabels:
      app: app-61
  template:
    metadata:
      labels:
        app: app-61
    spec:
      containers:
      - image: nginx
        name: nginx
