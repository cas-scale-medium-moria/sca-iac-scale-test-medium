apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-66
spec:
  selector:
    matchLabels:
      app: app-66
  template:
    metadata:
      labels:
        app: app-66
    spec:
      containers:
      - image: nginx
        name: nginx
