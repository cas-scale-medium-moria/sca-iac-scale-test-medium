apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-316
spec:
  selector:
    matchLabels:
      app: app-316
  template:
    metadata:
      labels:
        app: app-316
    spec:
      containers:
      - image: nginx
        name: nginx
