apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-205
spec:
  selector:
    matchLabels:
      app: app-205
  template:
    metadata:
      labels:
        app: app-205
    spec:
      containers:
      - image: nginx
        name: nginx
