apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-455
spec:
  selector:
    matchLabels:
      app: app-455
  template:
    metadata:
      labels:
        app: app-455
    spec:
      containers:
      - image: nginx
        name: nginx
