apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-308
spec:
  selector:
    matchLabels:
      app: app-308
  template:
    metadata:
      labels:
        app: app-308
    spec:
      containers:
      - image: nginx
        name: nginx
