apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-470
spec:
  selector:
    matchLabels:
      app: app-470
  template:
    metadata:
      labels:
        app: app-470
    spec:
      containers:
      - image: nginx
        name: nginx
