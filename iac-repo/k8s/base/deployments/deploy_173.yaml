apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-172
spec:
  selector:
    matchLabels:
      app: app-172
  template:
    metadata:
      labels:
        app: app-172
    spec:
      containers:
      - image: nginx
        name: nginx
