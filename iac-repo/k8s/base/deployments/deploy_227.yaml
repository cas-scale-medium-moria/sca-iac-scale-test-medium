apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-226
spec:
  selector:
    matchLabels:
      app: app-226
  template:
    metadata:
      labels:
        app: app-226
    spec:
      containers:
      - image: nginx
        name: nginx
