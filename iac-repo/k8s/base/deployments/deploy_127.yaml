apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-126
spec:
  selector:
    matchLabels:
      app: app-126
  template:
    metadata:
      labels:
        app: app-126
    spec:
      containers:
      - image: nginx
        name: nginx
