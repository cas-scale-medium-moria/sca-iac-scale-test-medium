apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-148
spec:
  selector:
    matchLabels:
      app: app-148
  template:
    metadata:
      labels:
        app: app-148
    spec:
      containers:
      - image: nginx
        name: nginx
