apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-63
spec:
  selector:
    matchLabels:
      app: app-63
  template:
    metadata:
      labels:
        app: app-63
    spec:
      containers:
      - image: nginx
        name: nginx
