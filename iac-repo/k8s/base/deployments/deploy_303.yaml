apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-302
spec:
  selector:
    matchLabels:
      app: app-302
  template:
    metadata:
      labels:
        app: app-302
    spec:
      containers:
      - image: nginx
        name: nginx
