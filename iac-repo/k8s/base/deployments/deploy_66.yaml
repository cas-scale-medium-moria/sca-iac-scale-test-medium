apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-65
spec:
  selector:
    matchLabels:
      app: app-65
  template:
    metadata:
      labels:
        app: app-65
    spec:
      containers:
      - image: nginx
        name: nginx
