apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-428
spec:
  selector:
    matchLabels:
      app: app-428
  template:
    metadata:
      labels:
        app: app-428
    spec:
      containers:
      - image: nginx
        name: nginx
