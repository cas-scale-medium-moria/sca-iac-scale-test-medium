apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-284
spec:
  selector:
    matchLabels:
      app: app-284
  template:
    metadata:
      labels:
        app: app-284
    spec:
      containers:
      - image: nginx
        name: nginx
