apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-413
spec:
  selector:
    matchLabels:
      app: app-413
  template:
    metadata:
      labels:
        app: app-413
    spec:
      containers:
      - image: nginx
        name: nginx
