apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-232
spec:
  selector:
    matchLabels:
      app: app-232
  template:
    metadata:
      labels:
        app: app-232
    spec:
      containers:
      - image: nginx
        name: nginx
