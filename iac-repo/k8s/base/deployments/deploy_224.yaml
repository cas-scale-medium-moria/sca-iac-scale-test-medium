apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-223
spec:
  selector:
    matchLabels:
      app: app-223
  template:
    metadata:
      labels:
        app: app-223
    spec:
      containers:
      - image: nginx
        name: nginx
