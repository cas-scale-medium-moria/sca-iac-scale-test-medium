apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-210
spec:
  selector:
    matchLabels:
      app: app-210
  template:
    metadata:
      labels:
        app: app-210
    spec:
      containers:
      - image: nginx
        name: nginx
