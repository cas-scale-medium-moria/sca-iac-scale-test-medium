apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-407
spec:
  selector:
    matchLabels:
      app: app-407
  template:
    metadata:
      labels:
        app: app-407
    spec:
      containers:
      - image: nginx
        name: nginx
