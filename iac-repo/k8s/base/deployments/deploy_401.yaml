apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-400
spec:
  selector:
    matchLabels:
      app: app-400
  template:
    metadata:
      labels:
        app: app-400
    spec:
      containers:
      - image: nginx
        name: nginx
