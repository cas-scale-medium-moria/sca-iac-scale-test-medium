apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-395
spec:
  selector:
    matchLabels:
      app: app-395
  template:
    metadata:
      labels:
        app: app-395
    spec:
      containers:
      - image: nginx
        name: nginx
