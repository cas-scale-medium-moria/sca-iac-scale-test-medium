apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-5
spec:
  selector:
    matchLabels:
      app: app-5
  template:
    metadata:
      labels:
        app: app-5
    spec:
      containers:
      - image: nginx
        name: nginx
