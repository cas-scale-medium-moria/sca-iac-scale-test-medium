apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-4
spec:
  selector:
    matchLabels:
      app: app-4
  template:
    metadata:
      labels:
        app: app-4
    spec:
      containers:
      - image: nginx
        name: nginx
