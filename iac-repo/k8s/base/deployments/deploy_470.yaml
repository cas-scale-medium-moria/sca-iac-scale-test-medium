apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-469
spec:
  selector:
    matchLabels:
      app: app-469
  template:
    metadata:
      labels:
        app: app-469
    spec:
      containers:
      - image: nginx
        name: nginx
