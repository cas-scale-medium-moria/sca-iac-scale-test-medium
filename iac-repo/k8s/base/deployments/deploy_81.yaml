apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-80
spec:
  selector:
    matchLabels:
      app: app-80
  template:
    metadata:
      labels:
        app: app-80
    spec:
      containers:
      - image: nginx
        name: nginx
