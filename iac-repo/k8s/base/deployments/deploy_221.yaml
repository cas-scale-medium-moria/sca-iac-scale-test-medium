apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-220
spec:
  selector:
    matchLabels:
      app: app-220
  template:
    metadata:
      labels:
        app: app-220
    spec:
      containers:
      - image: nginx
        name: nginx
