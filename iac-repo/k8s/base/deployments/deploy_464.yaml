apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-463
spec:
  selector:
    matchLabels:
      app: app-463
  template:
    metadata:
      labels:
        app: app-463
    spec:
      containers:
      - image: nginx
        name: nginx
