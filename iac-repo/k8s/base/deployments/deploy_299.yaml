apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-298
spec:
  selector:
    matchLabels:
      app: app-298
  template:
    metadata:
      labels:
        app: app-298
    spec:
      containers:
      - image: nginx
        name: nginx
