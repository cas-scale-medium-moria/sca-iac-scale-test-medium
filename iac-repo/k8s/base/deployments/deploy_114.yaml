apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-113
spec:
  selector:
    matchLabels:
      app: app-113
  template:
    metadata:
      labels:
        app: app-113
    spec:
      containers:
      - image: nginx
        name: nginx
