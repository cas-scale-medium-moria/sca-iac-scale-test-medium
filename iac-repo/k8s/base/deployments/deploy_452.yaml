apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-451
spec:
  selector:
    matchLabels:
      app: app-451
  template:
    metadata:
      labels:
        app: app-451
    spec:
      containers:
      - image: nginx
        name: nginx
