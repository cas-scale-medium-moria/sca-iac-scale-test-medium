apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-209
spec:
  selector:
    matchLabels:
      app: app-209
  template:
    metadata:
      labels:
        app: app-209
    spec:
      containers:
      - image: nginx
        name: nginx
