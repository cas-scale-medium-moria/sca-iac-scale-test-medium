apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-203
spec:
  selector:
    matchLabels:
      app: app-203
  template:
    metadata:
      labels:
        app: app-203
    spec:
      containers:
      - image: nginx
        name: nginx
