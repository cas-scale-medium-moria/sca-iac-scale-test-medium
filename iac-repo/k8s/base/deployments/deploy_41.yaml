apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-40
spec:
  selector:
    matchLabels:
      app: app-40
  template:
    metadata:
      labels:
        app: app-40
    spec:
      containers:
      - image: nginx
        name: nginx
