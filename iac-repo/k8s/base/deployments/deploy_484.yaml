apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-483
spec:
  selector:
    matchLabels:
      app: app-483
  template:
    metadata:
      labels:
        app: app-483
    spec:
      containers:
      - image: nginx
        name: nginx
