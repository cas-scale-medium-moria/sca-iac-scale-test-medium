apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-100
spec:
  selector:
    matchLabels:
      app: app-100
  template:
    metadata:
      labels:
        app: app-100
    spec:
      containers:
      - image: nginx
        name: nginx
