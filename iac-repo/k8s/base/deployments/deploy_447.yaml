apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-446
spec:
  selector:
    matchLabels:
      app: app-446
  template:
    metadata:
      labels:
        app: app-446
    spec:
      containers:
      - image: nginx
        name: nginx
