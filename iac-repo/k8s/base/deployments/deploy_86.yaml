apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-85
spec:
  selector:
    matchLabels:
      app: app-85
  template:
    metadata:
      labels:
        app: app-85
    spec:
      containers:
      - image: nginx
        name: nginx
