apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-430
spec:
  selector:
    matchLabels:
      app: app-430
  template:
    metadata:
      labels:
        app: app-430
    spec:
      containers:
      - image: nginx
        name: nginx
