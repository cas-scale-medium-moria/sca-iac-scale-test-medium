apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-213
spec:
  selector:
    matchLabels:
      app: app-213
  template:
    metadata:
      labels:
        app: app-213
    spec:
      containers:
      - image: nginx
        name: nginx
