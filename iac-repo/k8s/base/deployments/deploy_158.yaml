apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-157
spec:
  selector:
    matchLabels:
      app: app-157
  template:
    metadata:
      labels:
        app: app-157
    spec:
      containers:
      - image: nginx
        name: nginx
