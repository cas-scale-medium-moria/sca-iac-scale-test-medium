apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-352
spec:
  selector:
    matchLabels:
      app: app-352
  template:
    metadata:
      labels:
        app: app-352
    spec:
      containers:
      - image: nginx
        name: nginx
