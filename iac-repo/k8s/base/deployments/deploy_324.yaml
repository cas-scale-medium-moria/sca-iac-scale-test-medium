apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-323
spec:
  selector:
    matchLabels:
      app: app-323
  template:
    metadata:
      labels:
        app: app-323
    spec:
      containers:
      - image: nginx
        name: nginx
