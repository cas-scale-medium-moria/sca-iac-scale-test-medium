apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-0
spec:
  selector:
    matchLabels:
      app: app-0
  template:
    metadata:
      labels:
        app: app-0
    spec:
      containers:
      - image: nginx
        name: nginx
