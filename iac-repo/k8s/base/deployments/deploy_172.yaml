apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-171
spec:
  selector:
    matchLabels:
      app: app-171
  template:
    metadata:
      labels:
        app: app-171
    spec:
      containers:
      - image: nginx
        name: nginx
