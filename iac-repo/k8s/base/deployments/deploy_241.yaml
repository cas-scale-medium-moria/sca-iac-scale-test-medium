apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-240
spec:
  selector:
    matchLabels:
      app: app-240
  template:
    metadata:
      labels:
        app: app-240
    spec:
      containers:
      - image: nginx
        name: nginx
