apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-110
spec:
  selector:
    matchLabels:
      app: app-110
  template:
    metadata:
      labels:
        app: app-110
    spec:
      containers:
      - image: nginx
        name: nginx
