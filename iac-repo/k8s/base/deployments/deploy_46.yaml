apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-45
spec:
  selector:
    matchLabels:
      app: app-45
  template:
    metadata:
      labels:
        app: app-45
    spec:
      containers:
      - image: nginx
        name: nginx
