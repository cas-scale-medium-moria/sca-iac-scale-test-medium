apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-429
spec:
  selector:
    matchLabels:
      app: app-429
  template:
    metadata:
      labels:
        app: app-429
    spec:
      containers:
      - image: nginx
        name: nginx
