apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-255
spec:
  selector:
    matchLabels:
      app: app-255
  template:
    metadata:
      labels:
        app: app-255
    spec:
      containers:
      - image: nginx
        name: nginx
