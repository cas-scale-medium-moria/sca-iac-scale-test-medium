apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-25
spec:
  selector:
    matchLabels:
      app: app-25
  template:
    metadata:
      labels:
        app: app-25
    spec:
      containers:
      - image: nginx
        name: nginx
