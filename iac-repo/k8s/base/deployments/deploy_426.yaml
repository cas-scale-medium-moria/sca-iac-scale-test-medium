apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-425
spec:
  selector:
    matchLabels:
      app: app-425
  template:
    metadata:
      labels:
        app: app-425
    spec:
      containers:
      - image: nginx
        name: nginx
