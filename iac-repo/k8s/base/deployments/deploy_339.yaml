apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-338
spec:
  selector:
    matchLabels:
      app: app-338
  template:
    metadata:
      labels:
        app: app-338
    spec:
      containers:
      - image: nginx
        name: nginx
