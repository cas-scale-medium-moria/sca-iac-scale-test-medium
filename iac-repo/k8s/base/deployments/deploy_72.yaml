apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-71
spec:
  selector:
    matchLabels:
      app: app-71
  template:
    metadata:
      labels:
        app: app-71
    spec:
      containers:
      - image: nginx
        name: nginx
