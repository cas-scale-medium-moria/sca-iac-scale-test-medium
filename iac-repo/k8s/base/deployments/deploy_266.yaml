apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-265
spec:
  selector:
    matchLabels:
      app: app-265
  template:
    metadata:
      labels:
        app: app-265
    spec:
      containers:
      - image: nginx
        name: nginx
