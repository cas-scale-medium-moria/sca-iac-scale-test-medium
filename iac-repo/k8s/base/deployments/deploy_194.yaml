apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-193
spec:
  selector:
    matchLabels:
      app: app-193
  template:
    metadata:
      labels:
        app: app-193
    spec:
      containers:
      - image: nginx
        name: nginx
