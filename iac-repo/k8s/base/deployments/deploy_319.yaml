apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-318
spec:
  selector:
    matchLabels:
      app: app-318
  template:
    metadata:
      labels:
        app: app-318
    spec:
      containers:
      - image: nginx
        name: nginx
