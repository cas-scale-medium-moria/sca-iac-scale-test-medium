apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-78
spec:
  selector:
    matchLabels:
      app: app-78
  template:
    metadata:
      labels:
        app: app-78
    spec:
      containers:
      - image: nginx
        name: nginx
