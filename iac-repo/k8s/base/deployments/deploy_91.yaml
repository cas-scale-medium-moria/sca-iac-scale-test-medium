apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-90
spec:
  selector:
    matchLabels:
      app: app-90
  template:
    metadata:
      labels:
        app: app-90
    spec:
      containers:
      - image: nginx
        name: nginx
