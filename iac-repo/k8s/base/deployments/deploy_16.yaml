apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-15
spec:
  selector:
    matchLabels:
      app: app-15
  template:
    metadata:
      labels:
        app: app-15
    spec:
      containers:
      - image: nginx
        name: nginx
