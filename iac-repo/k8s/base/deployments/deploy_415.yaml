apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-414
spec:
  selector:
    matchLabels:
      app: app-414
  template:
    metadata:
      labels:
        app: app-414
    spec:
      containers:
      - image: nginx
        name: nginx
