apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-345
spec:
  selector:
    matchLabels:
      app: app-345
  template:
    metadata:
      labels:
        app: app-345
    spec:
      containers:
      - image: nginx
        name: nginx
