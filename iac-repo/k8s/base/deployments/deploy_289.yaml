apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-288
spec:
  selector:
    matchLabels:
      app: app-288
  template:
    metadata:
      labels:
        app: app-288
    spec:
      containers:
      - image: nginx
        name: nginx
