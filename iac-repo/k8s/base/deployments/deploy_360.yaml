apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-359
spec:
  selector:
    matchLabels:
      app: app-359
  template:
    metadata:
      labels:
        app: app-359
    spec:
      containers:
      - image: nginx
        name: nginx
