apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-271
spec:
  selector:
    matchLabels:
      app: app-271
  template:
    metadata:
      labels:
        app: app-271
    spec:
      containers:
      - image: nginx
        name: nginx
