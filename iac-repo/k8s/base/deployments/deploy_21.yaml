apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-20
spec:
  selector:
    matchLabels:
      app: app-20
  template:
    metadata:
      labels:
        app: app-20
    spec:
      containers:
      - image: nginx
        name: nginx
