apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-477
spec:
  selector:
    matchLabels:
      app: app-477
  template:
    metadata:
      labels:
        app: app-477
    spec:
      containers:
      - image: nginx
        name: nginx
