apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-106
spec:
  selector:
    matchLabels:
      app: app-106
  template:
    metadata:
      labels:
        app: app-106
    spec:
      containers:
      - image: nginx
        name: nginx
