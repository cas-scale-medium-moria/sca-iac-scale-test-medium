apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-351
spec:
  selector:
    matchLabels:
      app: app-351
  template:
    metadata:
      labels:
        app: app-351
    spec:
      containers:
      - image: nginx
        name: nginx
