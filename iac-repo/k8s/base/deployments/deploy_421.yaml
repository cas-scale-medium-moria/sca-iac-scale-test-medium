apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-420
spec:
  selector:
    matchLabels:
      app: app-420
  template:
    metadata:
      labels:
        app: app-420
    spec:
      containers:
      - image: nginx
        name: nginx
