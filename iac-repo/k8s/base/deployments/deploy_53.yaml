apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-52
spec:
  selector:
    matchLabels:
      app: app-52
  template:
    metadata:
      labels:
        app: app-52
    spec:
      containers:
      - image: nginx
        name: nginx
