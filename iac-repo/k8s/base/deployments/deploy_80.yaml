apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-79
spec:
  selector:
    matchLabels:
      app: app-79
  template:
    metadata:
      labels:
        app: app-79
    spec:
      containers:
      - image: nginx
        name: nginx
