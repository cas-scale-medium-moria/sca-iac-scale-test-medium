apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-215
spec:
  selector:
    matchLabels:
      app: app-215
  template:
    metadata:
      labels:
        app: app-215
    spec:
      containers:
      - image: nginx
        name: nginx
