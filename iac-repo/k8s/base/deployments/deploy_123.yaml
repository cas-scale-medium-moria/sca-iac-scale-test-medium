apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-122
spec:
  selector:
    matchLabels:
      app: app-122
  template:
    metadata:
      labels:
        app: app-122
    spec:
      containers:
      - image: nginx
        name: nginx
