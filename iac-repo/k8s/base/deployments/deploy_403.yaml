apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-402
spec:
  selector:
    matchLabels:
      app: app-402
  template:
    metadata:
      labels:
        app: app-402
    spec:
      containers:
      - image: nginx
        name: nginx
