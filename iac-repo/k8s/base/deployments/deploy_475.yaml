apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-474
spec:
  selector:
    matchLabels:
      app: app-474
  template:
    metadata:
      labels:
        app: app-474
    spec:
      containers:
      - image: nginx
        name: nginx
