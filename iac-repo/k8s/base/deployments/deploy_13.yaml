apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-12
spec:
  selector:
    matchLabels:
      app: app-12
  template:
    metadata:
      labels:
        app: app-12
    spec:
      containers:
      - image: nginx
        name: nginx
