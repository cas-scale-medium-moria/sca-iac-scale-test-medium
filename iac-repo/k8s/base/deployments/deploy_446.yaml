apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-445
spec:
  selector:
    matchLabels:
      app: app-445
  template:
    metadata:
      labels:
        app: app-445
    spec:
      containers:
      - image: nginx
        name: nginx
