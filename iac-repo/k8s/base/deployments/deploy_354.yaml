apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-353
spec:
  selector:
    matchLabels:
      app: app-353
  template:
    metadata:
      labels:
        app: app-353
    spec:
      containers:
      - image: nginx
        name: nginx
