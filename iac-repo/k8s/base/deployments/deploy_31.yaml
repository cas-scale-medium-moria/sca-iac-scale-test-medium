apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-30
spec:
  selector:
    matchLabels:
      app: app-30
  template:
    metadata:
      labels:
        app: app-30
    spec:
      containers:
      - image: nginx
        name: nginx
