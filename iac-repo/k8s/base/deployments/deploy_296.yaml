apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-295
spec:
  selector:
    matchLabels:
      app: app-295
  template:
    metadata:
      labels:
        app: app-295
    spec:
      containers:
      - image: nginx
        name: nginx
