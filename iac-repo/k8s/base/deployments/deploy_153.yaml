apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-152
spec:
  selector:
    matchLabels:
      app: app-152
  template:
    metadata:
      labels:
        app: app-152
    spec:
      containers:
      - image: nginx
        name: nginx
