apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-53
spec:
  selector:
    matchLabels:
      app: app-53
  template:
    metadata:
      labels:
        app: app-53
    spec:
      containers:
      - image: nginx
        name: nginx
