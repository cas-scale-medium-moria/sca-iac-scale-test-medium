apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-256
spec:
  selector:
    matchLabels:
      app: app-256
  template:
    metadata:
      labels:
        app: app-256
    spec:
      containers:
      - image: nginx
        name: nginx
