apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-327
spec:
  selector:
    matchLabels:
      app: app-327
  template:
    metadata:
      labels:
        app: app-327
    spec:
      containers:
      - image: nginx
        name: nginx
