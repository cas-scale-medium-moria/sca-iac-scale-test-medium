apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-422
spec:
  selector:
    matchLabels:
      app: app-422
  template:
    metadata:
      labels:
        app: app-422
    spec:
      containers:
      - image: nginx
        name: nginx
