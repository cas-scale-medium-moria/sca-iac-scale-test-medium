apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-175
spec:
  selector:
    matchLabels:
      app: app-175
  template:
    metadata:
      labels:
        app: app-175
    spec:
      containers:
      - image: nginx
        name: nginx
