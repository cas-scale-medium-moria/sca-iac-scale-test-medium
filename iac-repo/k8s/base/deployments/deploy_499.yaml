apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-498
spec:
  selector:
    matchLabels:
      app: app-498
  template:
    metadata:
      labels:
        app: app-498
    spec:
      containers:
      - image: nginx
        name: nginx
