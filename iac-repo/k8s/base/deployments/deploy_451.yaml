apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-450
spec:
  selector:
    matchLabels:
      app: app-450
  template:
    metadata:
      labels:
        app: app-450
    spec:
      containers:
      - image: nginx
        name: nginx
