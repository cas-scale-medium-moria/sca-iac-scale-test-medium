apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-435
spec:
  selector:
    matchLabels:
      app: app-435
  template:
    metadata:
      labels:
        app: app-435
    spec:
      containers:
      - image: nginx
        name: nginx
