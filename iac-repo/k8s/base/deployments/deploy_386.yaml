apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-385
spec:
  selector:
    matchLabels:
      app: app-385
  template:
    metadata:
      labels:
        app: app-385
    spec:
      containers:
      - image: nginx
        name: nginx
