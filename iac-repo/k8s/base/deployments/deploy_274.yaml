apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-273
spec:
  selector:
    matchLabels:
      app: app-273
  template:
    metadata:
      labels:
        app: app-273
    spec:
      containers:
      - image: nginx
        name: nginx
