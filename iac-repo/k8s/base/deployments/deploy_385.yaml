apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-384
spec:
  selector:
    matchLabels:
      app: app-384
  template:
    metadata:
      labels:
        app: app-384
    spec:
      containers:
      - image: nginx
        name: nginx
