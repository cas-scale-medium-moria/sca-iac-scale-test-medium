apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-120
spec:
  selector:
    matchLabels:
      app: app-120
  template:
    metadata:
      labels:
        app: app-120
    spec:
      containers:
      - image: nginx
        name: nginx
