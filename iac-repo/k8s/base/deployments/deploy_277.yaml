apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-276
spec:
  selector:
    matchLabels:
      app: app-276
  template:
    metadata:
      labels:
        app: app-276
    spec:
      containers:
      - image: nginx
        name: nginx
