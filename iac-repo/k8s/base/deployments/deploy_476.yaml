apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-475
spec:
  selector:
    matchLabels:
      app: app-475
  template:
    metadata:
      labels:
        app: app-475
    spec:
      containers:
      - image: nginx
        name: nginx
