apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-18
spec:
  selector:
    matchLabels:
      app: app-18
  template:
    metadata:
      labels:
        app: app-18
    spec:
      containers:
      - image: nginx
        name: nginx
