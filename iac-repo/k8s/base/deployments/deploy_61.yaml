apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-60
spec:
  selector:
    matchLabels:
      app: app-60
  template:
    metadata:
      labels:
        app: app-60
    spec:
      containers:
      - image: nginx
        name: nginx
