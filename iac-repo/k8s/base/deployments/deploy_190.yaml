apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-189
spec:
  selector:
    matchLabels:
      app: app-189
  template:
    metadata:
      labels:
        app: app-189
    spec:
      containers:
      - image: nginx
        name: nginx
