apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-249
spec:
  selector:
    matchLabels:
      app: app-249
  template:
    metadata:
      labels:
        app: app-249
    spec:
      containers:
      - image: nginx
        name: nginx
