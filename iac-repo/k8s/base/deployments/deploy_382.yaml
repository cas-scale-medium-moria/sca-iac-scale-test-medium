apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-381
spec:
  selector:
    matchLabels:
      app: app-381
  template:
    metadata:
      labels:
        app: app-381
    spec:
      containers:
      - image: nginx
        name: nginx
