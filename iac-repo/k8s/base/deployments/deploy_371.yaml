apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-370
spec:
  selector:
    matchLabels:
      app: app-370
  template:
    metadata:
      labels:
        app: app-370
    spec:
      containers:
      - image: nginx
        name: nginx
