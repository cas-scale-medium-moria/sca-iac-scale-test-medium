apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-433
spec:
  selector:
    matchLabels:
      app: app-433
  template:
    metadata:
      labels:
        app: app-433
    spec:
      containers:
      - image: nginx
        name: nginx
