apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-383
spec:
  selector:
    matchLabels:
      app: app-383
  template:
    metadata:
      labels:
        app: app-383
    spec:
      containers:
      - image: nginx
        name: nginx
