apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-309
spec:
  selector:
    matchLabels:
      app: app-309
  template:
    metadata:
      labels:
        app: app-309
    spec:
      containers:
      - image: nginx
        name: nginx
