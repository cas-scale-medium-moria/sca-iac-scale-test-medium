apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-64
spec:
  selector:
    matchLabels:
      app: app-64
  template:
    metadata:
      labels:
        app: app-64
    spec:
      containers:
      - image: nginx
        name: nginx
