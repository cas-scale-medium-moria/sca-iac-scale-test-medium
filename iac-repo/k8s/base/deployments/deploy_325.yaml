apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-324
spec:
  selector:
    matchLabels:
      app: app-324
  template:
    metadata:
      labels:
        app: app-324
    spec:
      containers:
      - image: nginx
        name: nginx
