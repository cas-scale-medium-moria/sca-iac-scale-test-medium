apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-452
spec:
  selector:
    matchLabels:
      app: app-452
  template:
    metadata:
      labels:
        app: app-452
    spec:
      containers:
      - image: nginx
        name: nginx
